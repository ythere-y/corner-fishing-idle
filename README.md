# 背包钓鱼手记 🎣
<!-- 【修改】原标题「角落垂钓」→ 最终定名「背包钓鱼手记」（Backpack & Bait）；
     候选清单及命名理由见 docs/lore_backpacker.md。 -->

贴在桌面角落的一本「背包客钓鱼手记」——TA 在外面环游世界，边走边钓；你在这边，边挂机边看 TA 的旅程。Godot 4.6 · 2D。

> 世界观：一位刚离职/间隔年的年轻背包客（Jim 或 Ganie，由你选），背包一甩说走就走，
> 一路从家门口的河湾钓到深海、钓到没信号的溶洞。桌角这方小水塘，就是 TA 寄回来的「远程连线」。
> 玩法内核不变（自动挂机 / 收集 / 离线奖励），仅重新包装成「旅程手记」外壳。详见 `docs/lore_backpacker.md`。

## 首次开局：开场故事 → 选角色

<!-- 【新增】章节：说明新增的开场引导流程 -->

- 新玩家启动后先看一段**轻松语气的开场故事动画**（逐句翻页，可跳过），讲背包客为什么出发。
- 讲完后选**这次出发的人**：男生 **Jim**（路痴但运气好）或女生 **Ganie**（认路但不认输）。
- 选完直接进入原有的「新手引导」提示页，正式开始挂机——**老存档不受影响**，不会被拉回来重新选。
- 实现见 `character_data.gd`（新增角色数据）+ `main.gd`/`save_system.gd`/`ui_panels.gd` 的标注改动。

## 形态

- 无边框 + 置顶 + **逐像素透明**窗口（GL Compatibility 渲染器，无灰底），1040×720 透明画布。
- **可拖动**：在场景空白处按住左键拖到任意位置；默认右下角，位置存档。
- 空白透明区**鼠标穿透**到桌面；弹出面板在更大的透明窗里自由居中展开。
- 边缘羽化 shader，融进任意壁纸。

## 玩法

- **自动挂机钓鱼**：抛竿 → 等待 → 浮标咬钩下沉 + 涟漪 → 自动上鱼**入鱼篓** → 手动卖出变现，循环往复。
- **10 个钓点＝背包客走过的 10 个旅程站点**（第1站中国·家门口河湾 → 第10站斯洛文尼亚·溶洞尽头，路线：中国×4 → 菲律宾 → 马来西亚 → 新西兰 → 澳大利亚 → 冰岛 → 斯洛文尼亚），各有专属鱼池与随机事件，按累计渔获解锁；切换钓点改变鱼池、订单建议、事件与背景。地理跨度不再是 bug，是「TA 到底跑了多远」的正当理由。
- **219 种鱼 / 6 档品阶**（普通→神话），每条带生态标签（河/湖/溪/海/深/冷/夜/保护）决定出没钓点；权重抽取；**体重**影响价值，标「大·/巨物·」体型。缺专属图标时回退按品阶的通用鱼图标，不崩。
- **桌面宠物 🐱**：渔夫旁的小馋猫，上鱼时偶尔扒拉；2% 概率叼走鱼篓里**最廉价的一条杂鱼**（卖价 ≤30，珍藏/订单/锁定绝不动）当趣味事件，计入「猫税」统计与成就。
- **活水族箱 🐠**：「鱼缸」页签——把心爱渔获放进缸里（最多 8 条），缸内鱼沿正弦路径游动、按方向翻转，点鱼看它的纪录（最大体重/累计/首捕日/变体），可捞回鱼篓；鎏金/七彩变体带脉动光晕。每条 +观赏卖价加成（封顶 +5%）。最契合桌面摆件的健康非数值长线。
- **专注奖励**：窗口失焦且无操作时累计连续「专注」，满 25 / 50 分钟下一竿强制升级渔获（保底极品★★，再保底鎏金变体），切回或操作即清零，每日封顶——离开越久回来越有惊喜。
- **随机事件**（数据驱动 7 种）：鱼汛 / 晨雾 / 寒潮 / 顺水鱼群 / 涨潮 / 漂流木箱 / 保护鱼放流，低干扰，按钓点触发，叠加咬钩/价值/运气修正。
- **昼夜时段**（按真实时钟）：黎明 / 白昼 / 黄昏 / 夜晚自然流转；金色时段（晨昏）咬钩更勤、渔获略增值，夜晚静谧但偶有大物；场景随时段轻染色，HUD 显示。零存档。
- **星级品质**：无 / 上品★ / 极品★★ / 完美★★★，价值 ×1/×1.8/×4/×8，由**鱼饵**决定概率。
- **稀有变体**：每条鱼独立 roll 变体（普通/斑斓/鎏金/七彩，6%/1.2%/0.2%），价值 ×1/2/5/12，与星级正交。图鉴按种记录已见变体——**219×3=657 格**收集轴，超长线护城河（Chillquarium 式）。**诱饵/窝料**成长线按级整体抬高变体出现率（vbias，无窝料 0=基线），是这条收集轴的专属杠杆。
- **鱼篓背包**：起始 20 格、阶梯扩容至 55 格（8 级）；满篓暂停上钩（满篓兜底：自动折价兑现，留贵兑贱，珍藏不动）；**排序（最新/价值/品阶/重量）+ 订单鱼筛选**；收藏锁（锁定不被卖出/交付）。
- **成长**：四条线各管一轴——鱼竿（稀有度：缩短等待 + 提升高阶概率 + 鱼价加成，成本 200×2^级）；鱼饵（星级：蚯蚓→红虫→活虾→秘制饵）；鱼钩（产量：双钩，一定几率一次再上一条）；诱饵/窝料（稀有变体：无窝料→碎米窝→酒米窝→麝香窝料，按级抬高斑斓/鎏金/七彩出现率）。
- **每日订单**：每天 1 单，4 类（指定鱼种 / 指定品阶 / 大物≥Wkg / 完美品质），交付未锁渔获按鱼价 ×2.5；HUD 常驻进度，跨日刷新。
- **流动鱼贩**：随机限时收购时段，卖价 ×1.5（HUD 金色提示）。
- **图鉴**：219 种鱼，每种记录累计数 + 最大体重 + 首捕日 + 变体收集 + 长期徽章；钓点页显示各钓点鱼种收集进度。
- **成就**：30 项（渔获/财富/收集/品阶/品相/装备/陈列/变体/猫税线），达成发金币奖励。
- **离线收益**：回屏弹「离线小结」面板（时长 / 渔获数 / 最值钱一条 / 合计可卖 / 珍稀清单）；上限 8 小时、50% 效率、受背包格数截断。
- **音频**：UI / 抛竿 / 入水 / 咬钩 / 捕获 / 金币 / 升级 音效 + **分层环境音床**（多条循环音按「钓点生态 × 昼夜时段」配方淡入淡出；缺素材优雅退化为单层水声，详见 `docs/audio_asset_rules.md`）；设置可调静音 / 主音量 / 音效 / 环境音。

## 界面 / UI

- **入口收敛**：主界面右下角不放独立按钮——**点 HUD 金币栏**即开多功能面板「**垂钓手册**」；钓点签 / 订单签深链到对应页。
- **9 页签**：背包 · 图鉴 · 订单 · 成就 · 统计 · 钓点 · 鱼缸 · 装备 · 设置（升级与设置都并入页签）。
- **两种表面**：暗玻璃浮动面板 + 暖纸（米黄）卡片；单一暖金强调色，克制冷冬调。
- **品阶即信号**：鱼名颜色读品阶（普通灰→神话红），变体加 ◆ 宝石 + 图标辉光。
- **衬线展示声音**：面板标题、钓点名用 **Noto Serif SC**（`assets/fonts/`）呼应水彩卷轴气质；统计页顶部🏆「个人最佳」卡用 **30px 衬线等宽英雄数字**凸显史上最大体重；面板柔和投影。
- UI 规范对齐内部设计系统（颜色/字体/间距/圆角/两表面/品阶色/慢而无弹跳的动效，详见设计稿，未随游戏仓库发布）。
- **旅行手账风格方向**（新）：整体视觉走「手记装订」框架 + 每站独立地域色，详见 `docs/art_direction.md`。

## 运行

```sh
# 用 Godot 4.6 打开本目录，或命令行：
godot --path .

# 新克隆首次跑工具前先建缓存：
godot --headless --import
```

## 开发工具

```sh
# 无头回归测试（鱼数据 / 钓点·事件 / 鱼池隔离 / 分布 / 体重价格 / 背包卖鱼 / 排序 /
#   鱼竿·鱼饵·鱼钩·诱饵 / 成就 / 鱼贩 / 随机事件 / 每日订单 / 多钓点切换 / 满篓兜底 /
#   活水族箱 / 专注奖励 / 存档往返与旧档迁移 / 离线 / 动态层 / 测试模式）
godot --headless -s tools/validate_game.gd

# 引擎内视口截图（带 alpha，验证透明 + 各面板；输出 docs/img/）
godot --path . -s tools/dev_screenshot.gd

# 数值平衡探针（产出曲线 / 升级回本，只读）
godot --headless -s tools/balance_probe.gd
```

### 游戏内「测试模式」（开发期，逻辑见 `test_mode.gd`）

设置页顶部「测试模式（开发用）」开关切换 **游玩 / 测试** 两态。测试模式下设置页展开「🧪 测试台」：
强制昼夜时段、钓鱼提速（×3/×10/即时）、改金币、+1/拉满装备与背包、给指定鱼（鱼种×星级×变体）、
样本包/清空鱼篓、解锁全部钓点/点亮全图鉴、召唤鱼贩/重置订单/充能专注、立即触发随机事件。

**仅本会话生效、不写档**：进入即冻结磁盘存档，退出（切回游玩模式）从磁盘重载正式档、丢弃全部测试改动；
重启天然回到正式档——正式存档永不被测试数据污染。HUD 右上常驻「🧪测试」角标提示当前在测试模式。

### 开启新存档（开发期）

设置页底部「开启新存档」：删磁盘存档（主档/.bak/.tmp）+ 全状态复位默认开局（复用 `SaveSystem.apply({})`
迁移默认）+ 落盘干净档 + 重看引导（新：重新走一遍「开场故事→选角色」）。**两点确认防误触**（首点变红警示，3.5s 内再点执行）。开发阶段存档无价值，可随时清空重开。

## 结构

| 文件 | 职责 |
|---|---|
| `main.gd` | `class_name CornerFishing` 主控：透明窗/拖动、钓鱼状态机、经济、背包/成就/图鉴、HUD、卖鱼/升级、宠物、专注奖励、存档/离线；面板/事件/钓点/订单均薄壳委托下列模块 |
| `ui_panels.gd` | `class_name UIPanels`：所有面板/页签/卡片/样式构建（静态 g 模式）；含衬线标题、🏆英雄数字，**新增开场故事(story)/选角色(character)面板** |
| `character_data.gd` | **【新增】** `class_name CharacterData`：背包客角色数据（Jim / Ganie，纯人设文案，不影响数值） |
| `events.gd` | `class_name Events`：随机事件管理器（EventData 驱动） |
| `spots.gd` | `class_name Spots`：钓点鱼池/运气增值/解锁/切换/订单候选并集 |
| `orders.gd` | `class_name Orders`：每日订单/周目标/今日统计 |
| `decor.gd` | `class_name Decor`：陈列/活水族箱（缸位 8、观赏卖价加成） |
| `aquarium.gd` | `class_name Aquarium`：鱼缸视图——缸内鱼游动/翻转/点鱼看纪录/捞回/变体光晕 |
| `scene_painter.gd` | 场景绘制：底图 + 动态层（水光/雾/雪/涟漪/灯光/渔夫/宠物），按钓点切底图，缺资源回退程序化 |
| `fish_data.gd` | `class_name FishData`：219 鱼种、品阶、生态标签、权重、体重定价、星级、鱼饵、鱼钩、诱饵(变体偏置)、按钓点鱼池抽取 |
| `fish_lore.gd` | `class_name FishLore`：219 鱼种详情卡文案（desc/fact/wiki），desc 已改写为背包客手记语气 |
| `spot_data.gd` | `class_name SpotData`：钓点（鱼池 / 解锁条件 / 事件池 / 系数 / 背景），name/desc 已按旅程站点重新叙事 |
| `event_data.gd` | `class_name EventData`：随机事件（鱼汛/晨雾/寒潮/顺水/涨潮/漂流木箱/保护鱼放流） |
| `weather.gd` | `class_name Weather`：昼夜时段（黎明/白昼/黄昏/夜晚，按真实时钟，修正+染色） |
| `achievements.gd` | `class_name AchievementData`：成就配置（30 项） |
| `audio_manager.gd` | Autoload `Audio`：音效/环境音加载与播放、音量/静音、设置持久化 |
| `feather_mask.gdshader` | 屏幕坐标椭圆羽化遮罩，让场景向左上渐隐融桌面 |
| `assets/fonts/` | Noto Serif SC（衬线展示字体，标题/英雄数字用） |
| `assets/art/**` | 美术资源（不在工程逻辑里修改，只加载播放） |
| `tools/` | 无头测试 / 引擎内截图 / 平衡探针 |
| `docs/` | 调研、路线图、美术需求、白皮书 project_overview.html，**新增 `lore_backpacker.md`（世界观+开场故事脚本+项目名候选）** |

## 存档

`user://corner_fishing_save.json`（当前 **v13**，原 v12 基础上新增背包客角色字段）。每 10s 自动存 + 退出存 + 拖动后存 + 切钓点/陈列后存。
历次结构升级均向后兼容迁移（图鉴纪录轴、背包多元组、徽章、每日订单、多钓点、陈列、稀有变体、
**活水族箱 / 首捕日 / 专注奖励 v11、诱饵窝料 v12、背包客角色 v13 字段**），回归测试覆盖迁移。

## 路线图（未实现）

更多钓点（数据驱动，加一张干净底图 + spot_data 配置即可，渔夫/灯笼自动叠加）、稀有大鱼限时事件、
陈列架桌面小景渲染、自动卖鱼（商业化方向，**需拍板**）、打包 itch.io / Steam。
模块骨架（UIPanels/Events/Spots/Orders/Decor/Aquarium + SpotData/EventData）已就位，便于并行扩展。
